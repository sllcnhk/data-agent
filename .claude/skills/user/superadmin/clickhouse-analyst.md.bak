# ClickHouse Analyst Skill — SG 外呼业务分析规程

> **版本**: v6.1 | **更新时间**: 2026-03-20
> **适用环境**: ClickHouse SG（新加坡）及其他多环境（IDN/BR/MY/THAI/SG_AZURE）
> **知识库路径**: `customer_data/db_knowledge/`

---

## 数据库知识库

数据库元数据位于 `customer_data/db_knowledge/`：

```
customer_data/db_knowledge/
├── _index.md                          ← L1: 全局索引（首先读取）
├── relationships.md                   ← L2: 完整 ERD + 业务链路图
├── tables/                            ← L2: 各表详细文档
│   ├── dim_automatic_task.md              策略定义表
│   ├── dim_automatic_action.md            Action 节点定义（18 种类型）
│   ├── dim_automatic_plan.md              线索批次表
│   ├── dim_automatic_task_customer.md     策略线索明细（父子流转）
│   ├── dim_automatic_plan_action_task.md  核心桥梁表 ⭐
│   ├── dim_call_task.md                   外呼任务表 ⭐
│   ├── dim_call_task_customer.md          任务线索明细（重拨父子）
│   └── realtime_dwd_crm_call_record.md    呼叫记录宽表 ⭐（78 字段）
└── metrics/                           ← L3: 核心指标口径
    ├── cost_per_call.md
    ├── connect_rate.md
    └── monthly_bill.md
```

**按需加载原则**：
- 首先读取 `_index.md` 了解整体结构
- 分析具体表时，读取 `tables/<表名>.md`
- 跨表分析时，读取 `relationships.md`
- 计算指标时，读取 `metrics/<指标>.md`
- **不要一次性加载所有文件**

---

## 核心业务理解

### 外呼业务两种模式

| 特征 | 策略外呼 (Automation) | 手动外呼 (Manual) |
|------|----------------------|-----------------|
| 识别方式 | `automatic_task_id > 0` | `automatic_task_id IS NULL 或 = 0` |
| 任务表标识 | `dim_call_task.is_automatic_task = 1` | `is_automatic_task = 0 或 NULL` |
| 实际占比 | ~15.5%（近 7 天验证）| ~84.5% |
| 线索流转 | Plan 驱动，按 Action 节点流转，产生子线索 | 无流转，重拨产生子线索 |

### 策略外呼业务流程（重要）

```
① 策略定义（一次性）
   dim_automatic_task（策略）
   └─ dim_automatic_action（Action 节点，定义外呼/SMS/WA 等 18 种类型和流转条件）

② 线索导入（每批次）
   导入线索 → 创建 dim_automatic_plan（批次）
   → 为每条线索创建父线索 dim_automatic_task_customer（parent_id=0）

③ Plan 驱动流转（核心机制）
   Plan 按策略 Action 节点定义和流转条件，驱动线索逐批流转：
   → 线索到达 Action 节点
   → 生成子线索（dim_automatic_task_customer，parent_id=上一线索 ID）
   → 生成关联记录（dim_automatic_plan_action_task）
   → 创建具体执行任务（dim_call_task，type=1 时为外呼）

④ 任务执行
   dim_call_task → dim_call_task_customer → realtime_dwd_crm_call_record
```

### 父子线索机制（两种场景不同）

| 场景 | 父线索标识 | 子线索产生时机 |
|------|-----------|--------------|
| 策略线索（dim_automatic_task_customer）| `parent_automatic_task_customer_id = 0` | 流转到下一 Action 节点时 |
| 任务线索（dim_call_task_customer）| `parent_call_task_customer_id IS NULL` | 重拨配置触发时 |

---

## dim_automatic_task_customer 表详解

### 表用途

`dim_automatic_task_customer` 是 **Automation/Strategy 功能的线索明细表**，记录通过策略导入的所有线索及其流转过程。

### 核心字段说明

| 字段名 | 类型 | 业务含义 | 备注 |
|--------|------|----------|------|
| `automatic_task_customer_id` | Int64 | 线索 ID | 主键 |
| `automatic_task_id` | Nullable(Int64) | 策略任务 ID | 关联 dim_automatic_task |
| `task_id` | Nullable(Int64) | 任务 ID | 关联 dim_call_task，**父线索为 NULL** |
| `call_task_customer_id` | Nullable(Int64) | 呼叫任务线索 ID | **父线索为 NULL** |
| `parent_automatic_task_customer_id` | Nullable(Int64) | 父线索 ID | **核心字段：0=父线索，>0=子线索** |
| `enterprise_id` | Int64 | 企业 ID | 关联 Dim_Enterprise |
| `phone` | Nullable(String) | 客户手机号 | PII 数据，注意脱敏 |
| `type` | Nullable(Int8) | 线索类型 | 0=外呼，1=呼入，2=SMS，3=WhatsApp，4=Email |
| `extra_json` | Nullable(String) | 导入变量 | 存储导入时的自定义字段 |
| `status` | Nullable(String) | 线索状态 | - |
| `curr_node_id` | Nullable(String) | 当前节点 ID | 关联 dim_automatic_action |
| `prev_node_id` | Nullable(String) | 上一节点 ID | - |
| `next_node_id` | Nullable(String) | 下一节点 ID | - |
| `is_complete` | Nullable(Int8) | 是否完成 | 0=未完成，1=已完成 |
| `contact_batch_id` | Nullable(String) | 批次 ID | **父线索有值，子线索为 NULL** |
| `flow_steps` | Nullable(String) | 已执行的流程节点 | 记录流转路径 |
| `plan_id` | Int64 | 计划批次 ID | 关联 dim_automatic_plan |
| `action_id` | Nullable(String) | 当前 Action 节点 ID | **父线索为 NULL，子线索有值** |
| `process_id` | Nullable(Int64) | 流程 ID | - |
| `contact_id` | Nullable(String) | 触达 ID | - |
| `create_time` | DateTime | 创建时间 | 用于时间过滤 |
| `is_delete` | Nullable(Int8) | 软删除标志 | 0=有效，1=已删除 |

### 父线索 vs 子线索 判断逻辑

#### 父线索（初始导入的线索）

**判断条件**：
```sql
WHERE parent_automatic_task_customer_id = 0
```

**特征**：
| 字段 | 父线索值 | 说明 |
|------|---------|------|
| `parent_automatic_task_customer_id` | `0` | 核心标识 |
| `task_id` | `NULL` | 尚未关联具体任务 |
| `call_task_customer_id` | `NULL` | 尚未关联呼叫线索 |
| `contact_batch_id` | **有值** | 批次 ID，标识导入批次 |
| `action_id` | `NULL` | 尚未到达任何 Action 节点 |
| `create_time` | 导入时间 | 线索创建时间 |

**业务含义**：父线索是通过 Automation/Strategy 功能**批量导入的初始客户线索**，代表一个独立的客户触达需求。

#### 子线索（流转产生的线索）

**判断条件**：
```sql
WHERE parent_automatic_task_customer_id > 0
```

**特征**：
| 字段 | 子线索值 | 说明 |
|------|---------|------|
| `parent_automatic_task_customer_id` | **>0** | 指向父线索 ID |
| `task_id` | **有值** | 已关联具体任务 |
| `call_task_customer_id` | **有值** | 已关联呼叫线索 |
| `contact_batch_id` | `NULL` | 无批次 ID |
| `action_id` | **有值** | 当前所在的 Action 节点 ID |
| `create_time` | 流转时间 | 子线索创建时间（晚于父线索） |

**业务含义**：子线索是父线索在策略流程执行过程中，**流转到下一 Action 节点时产生的触达记录**。一个父线索可能产生多个子线索（多次触达）。

### 统计口径建议

| 统计场景 | 过滤条件 | 说明 |
|---------|---------|------|
| **线索导入量** | `parent_automatic_task_customer_id = 0` | 统计实际导入的客户数（去重） |
| **触达次数** | `parent_automatic_task_customer_id > 0` | 统计实际触达次数（含重拨） |
| **完整线索量** | 不区分父子 | 包含父线索和所有子线索（通常不推荐） |

### 查询示例

#### 示例 1：统计今日各企业导入的父线索数

```sql
SELECT 
    enterprise_id,
    count() AS parent_lead_count
FROM crm.dim_automatic_task_customer
PREWHERE create_time >= toDate('2026-03-20')
       AND create_time < addDays(toDate('2026-03-20'), 1)
WHERE is_delete = 0
  AND parent_automatic_task_customer_id = 0
GROUP BY enterprise_id
ORDER BY parent_lead_count DESC
LIMIT 10;
```

#### 示例 2：统计某父线索的所有流转记录

```sql
SELECT 
    automatic_task_customer_id,
    parent_automatic_task_customer_id,
    action_id,
    curr_node_id,
    create_time,
    flow_steps
FROM crm.dim_automatic_task_customer
WHERE is_delete = 0
  AND (
      automatic_task_customer_id = {父线索 ID}  -- 父线索本身
      OR parent_automatic_task_customer_id = {父线索 ID}  -- 所有子线索
  )
ORDER BY create_time ASC;
```

#### 示例 3：统计某策略的线索流转效率

```sql
SELECT 
    at.name AS strategy_name,
    countIf(datc.parent_automatic_task_customer_id = 0) AS total_parent_leads,
    countIf(datc.parent_automatic_task_customer_id > 0) AS total_child_leads,
    round(total_child_leads * 1.0 / total_parent_leads, 2) AS avg_touches_per_lead
FROM crm.dim_automatic_task_customer datc
JOIN crm.dim_automatic_task at ON datc.automatic_task_id = at.id
PREWHERE datc.create_time >= addDays(now(), -7)
WHERE datc.is_delete = 0
GROUP BY at.name
ORDER BY total_parent_leads DESC;
```

---

## 核心表与字段枚举（v6.0 更新）

### 1. 呼叫明细表：`crm.realtime_dwd_crm_call_record`

**用途**: 全量呼叫记录宽表，包含所有呼叫详情

**关键字段**:
| 字段名 | 类型 | 业务含义 | 备注 |
|--------|------|----------|------|
| `call_record_id` | String | 呼叫记录 ID | 主键 |
| `enterprise_id` | UInt64 | 企业 ID | 关联 Dim_Enterprise |
| `call_task_id` | UInt64 | 呼叫任务 ID | 关联 dim_call_task |
| `call_type` | UInt8 | 呼叫类型 | **1=外呼**, 2=呼入, 3=内部呼叫 |
| `call_code_type` | UInt8 | 呼叫结果码类型 | **见下方枚举** |
| `call_duration` | UInt32 | 通话时长（秒）| >0 表示接通 |
| `call_start_time` | DateTime | 呼叫开始时间 | 用于时间过滤 |
| `automatic_task_id` | UInt64 | 策略任务 ID | >0 表示策略外呼 |
| `is_delete` | UInt8 | 软删除标志 | 0=有效，1=已删除 |

**call_code_type 枚举（计费相关）**:
| 值 | 含义 | 是否计费 | 说明 |
|----|------|---------|------|
| **1** | Connected（接通） | ✅ 是 | 正常接通通话 |
| **16** | Connected（接通 - 其他） | ✅ 是 | 另一种接通状态 |
| **22** | Answering Machine（语音信箱） | ✅ 是 | 检测到语音信箱 |
| 其他 | 未接通/忙/无人接听等 | ❌ 否 | 不计费 |

**有效计费呼叫过滤条件**:
```sql
WHERE call_type = 1  -- 外呼
  AND call_code_type IN (1, 16, 22)  -- 接通或语音信箱
  AND is_delete = 0
```

### 2. 日账单表：`crm.realtime_ods_cost_data_report_day_bill_record`

**用途**: 每日账单明细记录，按企业 + 日期 + 业务类型汇总

**关键字段**:
| 字段名 | 类型 | 业务含义 | 备注 |
|--------|------|----------|------|
| `id` | UInt64 | 记录 ID | 主键 |
| `enterprise_id` | UInt64 | 企业 ID | 关联 Dim_Enterprise |
| `bill_date` | Date | 账单日期 | 分区键 |
| `bus_type` | UInt8 | 业务类型 | **见下方枚举** |
| `amount` | Decimal(18,4) | 账单金额 | 单位：元 |
| `create_time` | DateTime | 创建时间 | - |
| `is_delete` | UInt8 | 软删除标志 | 0=有效 |

**bus_type 枚举**:
| 值 | 含义 | 是否外呼相关 | 说明 |
|----|------|-------------|------|
| **1** | **Line Charge**（外呼通话费） | ✅ 是 | 按通话时长计费 |
| **2** | SMS（短信费） | ❌ 否 | 按条数计费 |
| **3** | WhatsApp | ❌ 否 | WA 消息费 |
| **4** | Email | ❌ 否 | 邮件发送费 |
| **5** | **BotCharge**（机器人外呼费） | ✅ 是 | AI 机器人外呼费 |

**外呼账单过滤条件**:
```sql
WHERE bus_type IN (1, 5)  -- Line Charge + BotCharge
  AND is_delete = 0
```

### 3. 企业维表：`data_statistics.Dim_Enterprise`

**用途**: 企业维度信息，用于获取企业名称和环境标识

**关键字段**:
| 字段名 | 类型 | 业务含义 | 备注 |
|--------|------|----------|------|
| `Enterprise_ID` | UInt64 | 企业 ID | 主键，关联各业务表 |
| `Enterprise_Name` | String | 企业名称 | 用于报告展示 |
| `Environment` | String | 环境标识 | 如 'SG', 'IDN', 'BR' 等 |
| `SaaS` | String | 部署模式 | SaaS/私有化 |

**企业 ID 关联原则**:
```sql
-- 严格 enterprise_id 相等关联
FROM crm.realtime_dwd_crm_call_record cr
JOIN data_statistics.Dim_Enterprise de 
  ON cr.enterprise_id = de.Enterprise_ID
WHERE de.Environment = '{当前环境}'
```

---

## 查询规范（强制执行）

### 1. 时间过滤（必须用 PREWHERE）

```sql
-- ✅ 正确
PREWHERE call_start_time >= addDays(now(), -30)

-- ❌ 禁止（全表扫描）
WHERE call_start_time >= addDays(now(), -30)
```

### 2. 软删除过滤（必须）

```sql
WHERE is_delete = 0
```

### 3. 禁止 SELECT *

`realtime_dwd_crm_call_record` 有 78 个字段，只选需要的列。

### 4. 统计线索数必须区分父子

```sql
-- 策略线索：统计实际线索数（只数父线索）
WHERE parent_automatic_task_customer_id = 0

-- 任务线索：统计实际线索数（只数父线索）
WHERE parent_call_task_customer_id IS NULL
```

### 5. 策略外呼必须过滤 type=1

```sql
-- dim_automatic_plan_action_task 中只有 type=1 才是外呼任务
WHERE apt.type = 1
```

### 6. 接通率统一口径

```sql
-- 接通 = call_duration > 0
countIf(call_duration > 0) / count() AS connect_rate
```

---

## 核心查询模板

### 模板 1：有呼叫无账单企业分析（标准模板）

```sql
-- 步骤 1: 获取有有效呼叫的企业列表
WITH call_enterprises AS (
    SELECT 
        cr.enterprise_id,
        count() AS total_calls,
        countIf(call_code_type IN (1, 16)) AS connected_calls,
        countIf(call_code_type = 22) AS answering_machine_calls
    FROM crm.realtime_dwd_crm_call_record cr
    PREWHERE cr.call_start_time >= toDate('2026-02-20') 
           AND cr.call_start_time < toDate('2026-03-20')
    WHERE cr.is_delete = 0
      AND cr.call_type = 1  -- 外呼
      AND cr.call_code_type IN (1, 16, 22)  -- 有效计费呼叫
    GROUP BY cr.enterprise_id
),
-- 步骤 2: 获取有外呼账单的企业列表
bill_enterprises AS (
    SELECT 
        br.enterprise_id,
        sum(br.amount) AS total_amount,
        count(DISTINCT br.bill_date) AS bill_days
    FROM crm.realtime_ods_cost_data_report_day_bill_record br
    PREWHERE br.bill_date >= toDate('2026-02-20')
           AND br.bill_date < toDate('2026-03-20')
    WHERE br.is_delete = 0
      AND br.bus_type IN (1, 5)  -- Line Charge + BotCharge
    GROUP BY br.enterprise_id
)
-- 步骤 3: 找出有呼叫无账单的企业
SELECT 
    ce.enterprise_id,
    de.Enterprise_Name,
    de.Environment,
    ce.total_calls,
    ce.connected_calls,
    ce.answering_machine_calls,
    '无账单' AS issue_type
FROM call_enterprises ce
LEFT JOIN bill_enterprises be ON ce.enterprise_id = be.enterprise_id
JOIN data_statistics.Dim_Enterprise de 
  ON ce.enterprise_id = de.Enterprise_ID AND de.Environment = '{环境标识}'
WHERE be.enterprise_id IS NULL
ORDER BY ce.total_calls DESC;
```

### 模板 2：策略外呼效果分析（推荐路径）

```sql
-- 利用宽表直接关联策略，无需多表 JOIN
SELECT
    cr.automatic_task_id,
    at.name                                              AS strategy_name,
    count()                                              AS total_calls,
    countIf(cr.call_duration > 0)                        AS connected_calls,
    round(countIf(cr.call_duration > 0) * 100.0 / count(), 2) AS connect_rate_pct,
    round(avg(if(cr.call_duration > 0, cr.call_duration, NULL)), 1) AS avg_duration_sec
FROM crm.realtime_dwd_crm_call_record cr
LEFT JOIN crm.dim_automatic_task at ON cr.automatic_task_id = at.id
PREWHERE cr.call_start_time >= addDays(now(), -30)
WHERE cr.is_delete = 0
  AND cr.automatic_task_id > 0
  AND cr.enterprise_id = {enterprise_id}
GROUP BY cr.automatic_task_id, at.name
ORDER BY total_calls DESC;
```

### 模板 3：今日 Automation 父线索 TOP 10

```sql
SELECT 
    de.Enterprise_Name,
    count() AS parent_lead_count
FROM crm.dim_automatic_task_customer datc
JOIN data_statistics.Dim_Enterprise de 
  ON datc.enterprise_id = de.Enterprise_ID
PREWHERE datc.create_time >= toDate('2026-03-20')
       AND datc.create_time < addDays(toDate('2026-03-20'), 1)
WHERE datc.is_delete = 0
  AND datc.parent_automatic_task_customer_id = 0
  AND de.Environment = 'SG'
GROUP BY de.Enterprise_Name
ORDER BY parent_lead_count DESC
LIMIT 10;
```

---

## 常见问题排查

### Q1: 为什么统计的线索数比实际导入的多很多？

**原因**：可能把子线索也统计进去了。

**解决**：确保使用 `parent_automatic_task_customer_id = 0` 过滤父线索。

### Q2: 如何追踪一个线索的完整流转路径？

**方法**：使用父线索 ID 查询所有相关记录：

```sql
SELECT 
    automatic_task_customer_id,
    parent_automatic_task_customer_id,
    action_id,
    curr_node_id,
    create_time,
    flow_steps
FROM crm.dim_automatic_task_customer
WHERE is_delete = 0
  AND (
      automatic_task_customer_id = {父线索 ID}
      OR parent_automatic_task_customer_id = {父线索 ID}
  )
ORDER BY create_time ASC;
```

### Q3: 如何计算某策略的平均触达次数？

**方法**：子线索数 / 父线索数

```sql
SELECT 
    countIf(parent_automatic_task_customer_id = 0) AS parent_count,
    countIf(parent_automatic_task_customer_id > 0) AS child_count,
    child_count * 1.0 / parent_count AS avg_touches
FROM crm.dim_automatic_task_customer
WHERE is_delete = 0
  AND automatic_task_id = {策略 ID};
```

---

## 版本更新记录

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v6.1 | 2026-03-20 | 新增 `dim_automatic_task_customer` 表详解章节，包含父/子线索判断逻辑、字段说明、查询示例 |
| v6.0 | 2026-03-20 | 初始版本，包含核心表结构、查询规范、模板 |
